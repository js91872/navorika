import Link from 'next/link';

export default function XmlWordEducationalContent({ direction }: { direction: 'xml-to-word' | 'word-to-xml' }) {
  const toWord = direction === 'xml-to-word';
  return <section className="mx-auto mt-12 max-w-5xl space-y-9 text-[var(--foreground)]"><div><h2 className="text-2xl font-black">{toWord ? 'How XML becomes a Word document' : 'How DOCX content becomes XML'}</h2><p className="mt-3 leading-7 text-[var(--muted-foreground)]">{toWord ? 'The converter validates XML, builds a bounded element tree, and maps that tree to real Office Open XML parts inside a DOCX ZIP package. Attributes remain visible in hierarchy and table output.' : 'A DOCX file is a ZIP package of XML parts. Structured mode reads the main document in order and emits a smaller document model; raw mode returns the original word/document.xml part unchanged.'}</p></div><div className="grid gap-5 md:grid-cols-2"><article className="rounded-2xl border border-slate-200 p-5 dark:border-slate-800"><h2 className="text-xl font-black">Formatting modes</h2><p className="mt-2 leading-7 text-[var(--muted-foreground)]">{toWord ? 'Hierarchy mode suits nested configuration or content. Table mode detects repeated sibling records. Raw mode presents formatted XML in a monospace Word layout.' : 'Clean XML is easier to integrate and inspect. Raw WordprocessingML preserves Word-specific markup but requires OOXML knowledge.'}</p></article><article className="rounded-2xl border border-slate-200 p-5 dark:border-slate-800"><h2 className="text-xl font-black">Important limitations</h2><p className="mt-2 leading-7 text-[var(--muted-foreground)]">{toWord ? 'XML has no universal visual layout, so conversion requires a presentation choice. Table detection only flattens scalar repeated records; deeply nested values stay better represented by hierarchy mode.' : 'Clean extraction is intentionally lossy: images, exact page layout, headers, footers, comments, tracked changes, footnotes, and advanced styles are not reproduced. It does not convert legacy binary .doc files.'}</p></article></div><div><h2 className="text-2xl font-black">Security and privacy</h2><p className="mt-3 leading-7 text-[var(--muted-foreground)]">Processing stays in the browser. XML DTD and entity declarations are rejected. DOCX packages are checked for path traversal, entry count, expanded size, and suspicious compression ratios before document parts are read. No file is permanently stored.</p></div><div className="rounded-2xl bg-slate-50 p-5 dark:bg-slate-900"><h2 className="text-xl font-black">Continue the workflow</h2><p className="mt-2 text-[var(--muted-foreground)]">Use the <Link href={toWord ? '/tools/word-to-xml-converter' : '/tools/xml-to-word-converter'} className="font-bold text-indigo-600 hover:underline">{toWord ? 'Word to XML converter' : 'XML to Word converter'}</Link> for the reverse direction, or clean markup first with the <Link href="/tools/markup-formatter" className="font-bold text-indigo-600 hover:underline">Markup Formatter</Link>.</p></div></section>;
}
