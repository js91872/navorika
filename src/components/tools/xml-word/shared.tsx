import { Download, LockKeyhole, RotateCcw } from 'lucide-react';

export function PrivacyNotice() { return <p className="flex items-start gap-2 rounded-xl bg-emerald-50 p-3 text-sm text-emerald-900 dark:bg-emerald-950/40 dark:text-emerald-200"><LockKeyhole className="mt-0.5 size-4 shrink-0" aria-hidden="true" /> Files are processed locally in this browser and are not uploaded to Navorika.</p>; }
export function ErrorNotice({ message }: { message: string }) { return <div role="alert" className="rounded-xl border border-red-200 bg-red-50 p-3 text-sm font-medium text-red-800 dark:border-red-900 dark:bg-red-950/40 dark:text-red-200">{message}</div>; }
export function ResultActions({ fileName, blob, warnings, onReset }: { fileName: string; blob: Blob; warnings: string[]; onReset: () => void }) {
  const download = () => { const url = URL.createObjectURL(blob); const anchor = document.createElement('a'); anchor.href = url; anchor.download = fileName; anchor.click(); window.setTimeout(() => URL.revokeObjectURL(url), 1_000); };
  return <div className="space-y-3 rounded-2xl border border-emerald-200 bg-emerald-50 p-4 dark:border-emerald-900 dark:bg-emerald-950/30"><p className="break-words font-bold text-emerald-900 dark:text-emerald-100">Ready: {fileName}</p>{warnings.map((warning) => <p key={warning} className="text-sm text-amber-800 dark:text-amber-200">{warning}</p>)}<div className="flex flex-col gap-2 sm:flex-row"><button type="button" onClick={download} className="inline-flex min-h-11 items-center justify-center gap-2 rounded-xl bg-emerald-600 px-4 py-2 font-bold text-white hover:bg-emerald-700"><Download className="size-4" /> Download</button><button type="button" onClick={onReset} className="inline-flex min-h-11 items-center justify-center gap-2 rounded-xl bg-white px-4 py-2 font-bold text-slate-800 ring-1 ring-slate-300 dark:bg-slate-900 dark:text-white dark:ring-slate-700"><RotateCcw className="size-4" /> Reset</button></div></div>;
}
