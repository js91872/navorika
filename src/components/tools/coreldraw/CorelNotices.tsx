import { Info, LockKeyhole } from 'lucide-react';

export function ServerPrivacyNotice() { return <p className="flex items-start gap-2 rounded-xl bg-emerald-50 p-3 text-sm text-emerald-900 dark:bg-emerald-950/40 dark:text-emerald-200"><LockKeyhole className="mt-0.5 size-4 shrink-0" /> Uploads are used only for conversion in an isolated temporary directory and are deleted automatically when the request finishes.</p>; }
export function LocalPrivacyNotice() { return <p className="flex items-start gap-2 rounded-xl bg-emerald-50 p-3 text-sm text-emerald-900 dark:bg-emerald-950/40 dark:text-emerald-200"><LockKeyhole className="mt-0.5 size-4 shrink-0" /> This image is processed locally in your browser and is not uploaded to Navorika.</p>; }
export function NativeCdrNotice() { return <div className="rounded-xl border border-amber-200 bg-amber-50 p-4 text-sm leading-6 text-amber-950 dark:border-amber-900 dark:bg-amber-950/30 dark:text-amber-100"><p className="flex items-center gap-2 font-black"><Info className="size-4" /> Native CDR vs CorelDRAW-ready output</p><p className="mt-1">CDR is CorelDRAW&apos;s proprietary native format. Navorika does not have a verified CDR writer, so it produces a genuine PDF, SVG, EPS, PNG, or JPG that CorelDRAW can import. Open the result in CorelDRAW, check fonts and pages, then choose Save As → CDR.</p></div>; }
export function CorelError({ message }: { message: string }) { return <div role="alert" className="rounded-xl border border-red-200 bg-red-50 p-3 text-sm font-semibold text-red-800 dark:border-red-900 dark:bg-red-950/30 dark:text-red-200">{message}</div>; }
