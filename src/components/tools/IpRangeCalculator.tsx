'use client';
import { Copy } from 'lucide-react'; import { useMemo,useState } from 'react'; import { calculateIpRange } from '@/lib/calculations/networkExpansion';
export default function IpRangeCalculator(){const[start,setStart]=useState('192.168.1.1');const[end,setEnd]=useState('192.168.1.254');const result=useMemo(()=>calculateIpRange(start,end),[start,end]);return <div className="grid gap-6 lg:grid-cols-2"><section className="rounded-3xl border border-[var(--border)] bg-[var(--card)] p-6"><h2 className="text-xl font-bold">IPv4 range</h2>{[['Start address',start,setStart],['End address',end,setEnd]].map(([label,value,setter])=><label key={label as string} className="mt-5 block text-sm font-semibold">{label as string}<input value={value as string} onChange={e=>(setter as (v:string)=>void)(e.target.value)} className="mt-2 w-full rounded-xl border border-[var(--border)] bg-[var(--background)] px-4 py-3 font-mono"/></label>)}{!result.valid&&<p role="alert" className="mt-4 text-red-600">{result.error}</p>}</section><section className="rounded-3xl border border-[var(--border)] bg-[var(--card)] p-6"><h2 className="text-xl font-bold">Range result</h2>{result.valid&&<><dl className="mt-4 space-y-3">{[['First',result.start],['Last',result.end],['Total addresses',result.count.toLocaleString()],['Smallest containing CIDR',result.containingCidr]].map(([label,value])=><div key={label} className="flex justify-between gap-4 border-b border-[var(--border)] py-2"><dt className="text-sm text-[var(--muted-foreground)]">{label}</dt><dd className="font-mono font-bold">{value}</dd></div>)}</dl><div className="mt-5 flex items-center justify-between"><h3 className="font-bold">Exact CIDR blocks</h3><button onClick={()=>void navigator.clipboard.writeText(result.exactCidrs.join('\n'))} className="inline-flex gap-2 text-sm"><Copy className="size-4"/>Copy</button></div><pre className="mt-3 max-h-64 overflow-auto rounded-xl bg-[var(--background)] p-4 text-sm">{result.exactCidrs.join('\n')}</pre></>}</section></div>}
