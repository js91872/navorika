import ToolUnavailable from '@/components/tools/ToolUnavailable';
export default function Page() { return <ToolUnavailable title="PNG to SVG Vectorizer" backHref="/categories/image-tools" backLabel="Back to Image Tools" unavailableReason="Wrapping a PNG inside an SVG does not convert raster pixels into vector paths." requirement="A genuine tracing engine with color reduction, path simplification, transparency handling, preview, and geometric validation is required." />; }
