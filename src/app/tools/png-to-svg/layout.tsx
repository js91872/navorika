import { createReviewMetadata } from '@/lib/seo/toolReview';
export const metadata = createReviewMetadata('PNG to SVG');
export default function Layout({ children }: Readonly<{ children: React.ReactNode }>) { return children; }
