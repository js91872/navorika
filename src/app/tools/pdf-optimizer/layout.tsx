import { ReactNode } from 'react';

export const metadata = {
  title: 'Compress PDF Online | Reduce PDF Size & Optimize for Web | Navorika',
  description: 'Free online PDF compressor tool. Reduce document file sizes and optimize PDF file streams for fast web viewing or email sharing securely.',
  keywords: 'compress pdf, reduce pdf size, optimize pdf for web, linearize pdf, fast web view, online pdf optimizer, private pdf compress',
};

export default function PDFOptimizerLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
