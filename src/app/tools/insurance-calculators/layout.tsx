import { ReactNode } from 'react';

export const metadata = {
  title: 'Human Life Value (HLV) & Insurance Calculator | Navorika',
  description: 'Evaluate your optimal term insurance coverage gap using our Human Life Value calculation algorithms.',
};

export default function InsuranceLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
