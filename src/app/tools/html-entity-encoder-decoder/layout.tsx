import ToolPageContent from '@/components/seo/ToolPageContent'; import { developerToolPages } from '@/data/tool-pages/developer'; import { createToolMetadata } from '@/lib/seo/toolPage'; const tool=developerToolPages['html-entity-encoder-decoder']; export const metadata=createToolMetadata(tool); export default function Layout({children}:{children:React.ReactNode}){return <>{children}<ToolPageContent tool={tool}/></>}
