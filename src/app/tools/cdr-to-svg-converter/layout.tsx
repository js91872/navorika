import ToolPageContent from '@/components/seo/ToolPageContent'; import { corelDrawToolPages } from '@/data/tool-pages/coreldraw'; import { createToolMetadata } from '@/lib/seo/toolPage'; const tool=corelDrawToolPages['cdr-to-svg-converter']; export const metadata=createToolMetadata(tool); export default function Layout({children}:{children:React.ReactNode}){return <>{children}<ToolPageContent tool={tool}/></>}
