import { ReactNode } from 'react';

export const metadata = {
  title: 'Credit Card EMI & Payoff Calculator | Navorika',
  description: 'Calculate credit card EMI, payoff timelines, interest accumulation, and evaluate balance transfers.',
};

export default function CreditCardLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
