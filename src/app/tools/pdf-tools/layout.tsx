import { ReactNode } from 'react';

export const metadata = {
  title: 'Merge PDF | Delete PDF Pages & Document Restructuring | Navorika',
  description: 'Free high-performance online PDF utility. Merge multiple files or delete unwanted pages instantly inside your browser with 100% data privacy.',
  keywords: 'merge pdf, combine pdf, delete pdf pages, remove pages from pdf, split pdf, online pdf editor, private pdf tool',
};

export default function PDFToolsLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
