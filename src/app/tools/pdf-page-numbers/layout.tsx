import { ReactNode } from 'react';

export const metadata = {
  title: 'Insert Page Numbers in PDF Online | Free PDF Stamper | Navorika',
  description: 'Add page numbers to your PDF documents automatically. Customize footer alignments and numbering formats securely in your browser.',
  keywords: 'add page numbers to pdf, insert page numbers pdf, number pages online, pdf footer stamper, client side pdf tool',
};

export default function PageNumbersLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
